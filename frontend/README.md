# Frontend — placeholder structure
# Run `npx create-next-app@latest ./` to initialize the Next.js project
# Then install: npm install zustand @tanstack/react-query recharts lucide-react

# After initialization, the following structure should be created:
# src/app/ — Next.js App Router pages
# src/components/ — UI components
# src/hooks/ — Custom React hooks
# src/services/ — API client layer
# src/store/ — State management (Zustand)
# src/lib/ — Utility functions
# src/types/ — TypeScript type definitions
